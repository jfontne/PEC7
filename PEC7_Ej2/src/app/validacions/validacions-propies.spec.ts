import { ValidacionsPropies } from './validacions-propies';

describe('ValidacionsPropies', () => {
  it('should create an instance', () => {
    expect(new ValidacionsPropies()).toBeTruthy();
  });
});
